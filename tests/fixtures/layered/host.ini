; Layer 2. What this one host changes about the base.
;
; Every value here is text, the way INI's always are, and it lands between two typed files.

; Nothing else in the chain says anything about these, and a comma is the list INI has.
notes = handwritten, uncommitted

[server]
backlog = 512

[database.pool]
max = 24
